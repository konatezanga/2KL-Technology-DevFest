/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com"], // ajoute tous les domaines externes que tu veux utiliser
  },
};

export default nextConfig;
